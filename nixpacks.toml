[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs_20"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt", "npm install"]

[phases.build]
cmds = ["npm run build:css"]
